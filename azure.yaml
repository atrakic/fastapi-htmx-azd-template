# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: fastapi-htmx-azd-template
metadata:
    template: azd-init@1.9.4
services:
    fastapi-htmx-azd-template:
        project: .
        host: containerapp
        language: python
        docker:
            path: Dockerfile
