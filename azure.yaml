# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: fastapi-htmx-stack
services:
  web:
    project: ./src
    language: py
    host: appservice
infra:
  provider: terraform
# azd pipeline config
#pipeline:
#  provider: ""
